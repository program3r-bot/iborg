# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=iborg
DB_PASSWORD=changeme
DB_NAME=iborg_dating

# JWT
JWT_SECRET=replace-with-strong-secret-min-32-chars
JWT_EXPIRES_IN=7d

# Server
PORT=3000
NODE_ENV=development

# Email (stub - configure SMTP for production)
EMAIL_FROM=noreply@example.com
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# CORS
CORS_ORIGIN=http://localhost:3000

# Cookie secret for CSRF
COOKIE_SECRET=replace-with-strong-cookie-secret
